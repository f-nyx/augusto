listeners {
  ConvertNumbers {
    triggers: [
      {
        name: "mention"
        type: "text"
        contains: ["@augusto", "@US034S5TQ"]
      }
    ]
  }

  Cancel {
    triggers: [
      {
        name: "cancel"
        type: "category"
        category: "cancel"
        score: 0.75
      }
    ]
  }
}
